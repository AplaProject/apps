contract CreditingDel {
    data {
        Id int
    }

    conditions {
    }

    action {
        var m map
		m["status"] = -1
		m["deleted"] = $block_time    

        DBUpdate("crediting_requests", Int($Id), m)
    }
}