SetTitle($land_records$ ($@1admin$))
SetVar(this, land_registry_records_admin)
SetVar(admin, 1)
SetVar(sales, 0)
SetVar(depart, 0)

If(And(#isSearch#==1, #str_search#>0)){
    SetVar(v_Where, {"delete": 0, "land_use": #str_search#})
}.Else{
    SetVar(v_Where, {"delete": 0})
}

Include(land_registry_records)