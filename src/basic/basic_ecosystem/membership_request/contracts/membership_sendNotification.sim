contract membership_sendNotification {
    data {
        request_id int
    }

    conditions {

    }

    action {
        var page_val string
        page_val = "notific_val_int=" + Str($request_id)

        var founder int
        founder = Int(EcosysParam("founder_account"))

        Notifications_Single_Send("icon_name,text_header,text_body,page_name,params_val,member_id", "fa-address-card", "Membership", "New request for membership", "request_view", page_val, founder)
    }
}