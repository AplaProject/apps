contract RolesRestore {
    data {
        rid int
    }
    
    conditions {        
        RolesAccessCheck("rid", $rid)
    }
    
    action {
        DBUpdate("roles", $rid, {deleted:0})
    }
}