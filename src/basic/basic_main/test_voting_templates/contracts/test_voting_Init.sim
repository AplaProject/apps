contract test_voting_Init{
    data{}
    
    conditions{
        ContractConditions("MainCondition")
    }
    
    action{
        // DBInsert("test", "app,case,value", "voting", "init Contract", "init OK")
    }
}