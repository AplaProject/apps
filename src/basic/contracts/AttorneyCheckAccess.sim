contract AttorneyCheckAccess {
    data {
        contract_name string
        wallet_id int
    }

    conditions {
        var attorney_id int
        attorney_id = DBFind("power_of_attorney").Where({"delete": 0, "contract": $contract_name, "address": $wallet_id, "key_id_attorney": $key_id}).One("id")
        if !attorney_id {
            warning Sprintf("You don't have access to wallet %s", $wallet_id)
        }
        $result = attorney_id
    }
    action {
    }
}