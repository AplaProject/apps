contract TokensDecDeposit {
    data {
        MemberId int
        Amount money
    }
    conditions {

    }
    action {
        var s map
        s["-deposit"] = $Amount
        DBUpdate("@1keys",Int($MemberId),s)
    }
}