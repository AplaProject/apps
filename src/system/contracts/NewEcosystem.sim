contract NewEcosystem {
    data {
        Name string
    }

    conditions {
        DeveloperCondition()

        if !$Name {
            warning LangRes("@1ecosystem_name_empty", "en")
        }
    }

    action {
        $result = CreateEcosystem($key_id, $Name)
    }
}