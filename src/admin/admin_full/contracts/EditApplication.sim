contract EditApplication {
    data {
        Id int
        Conditions string
    }

    conditions {
        ValidateCondition($Conditions, $ecosystem_id)
    }

    action {
        DBUpdate("applications", $Id, "conditions", $Conditions)
    }
}