contract extend_cost_len {
  data {
    Value string
  }

  conditions {
    if Size($Value) == 0 {
      warning "Value was not received"
    }
    if Int($Value) < 0 {
      warning "Value can not be less than zero"
    }
  }
}