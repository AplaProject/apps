contract EditColumn {
    data {
        TableName string
        Name string
        Perm string
    }

    conditions {
        ColumnCondition($TableName, $Name, "", $Perm)
    }

    action {
        PermColumn($TableName, $Name, $Perm)
    }
}