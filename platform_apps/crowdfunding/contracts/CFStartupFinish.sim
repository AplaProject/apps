contract CFStartupFinish {
    data {
        NotificId int
    }
    conditions{
        TODO: получить стартап из оповещения 
        $startup = DBFind("@1cf_startups").Where({id:$Id, status:$FINISHED, deleted_at:0}).Row()
       
    }
  
    action {
        @1CFAdmin("Id,Action", Int($startup["id"]), "finish")
    }
}